{
    "name": "qile-yang-website",
    "version": "0.1.0",
    "scripts": {
        "build": "npx postcss ./assets/css/tailwind.css -o ./assets/css/style.css",
        "start": "bundle exec jekyll serve",
        "dev": "bundle install && npm run build && npm run start"
    },
    "devDependencies": {
        "postcss-cli": "^11.0.0",
        "tailwindcss": "^3.4.15"
    },
    "dependencies": {
        "postcss": "^8.0.0",
        "tailwindcss": "^3.4.15",
        "autoprefixer": "^10.4.20",
        "postcss": "^8.4.49"
    }
}
